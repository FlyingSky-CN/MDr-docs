# 主题色设置

得益于 MDUI 框架，MDr 包含了 19 种主色和 16 种强调色。颜色参考请查看 [颜色和主题 - MDUI 文档](https://www.mdui.org/docs/color#color)。

## 主题强调色 ( Dark Mode )

该选项所配置的是 `Dark Mode` 开启时的主题强调色，与 `Dark Mode` 关闭时的主题强调色互不影响互不干扰。

## Chrome 顶栏颜色

即未开启 `Dark Mode` 时的 `meta["theme-color"]` 的值，为十六进制颜色码，默认为 `#ffffff`。